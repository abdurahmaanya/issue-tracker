import Head from "next/head";

export default function Header(){
    return (
      <Head>
        <title>Issue Tracker</title>
        <meta name="description" content="Issue Tracker" />
        <link rel="icon" href=".\issue-tracker-logo.ico" />
      </Head>
    );
}